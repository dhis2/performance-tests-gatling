name: Tracker Performance - BuildJet Shared

on:
  # I have enough data for now
  # schedule:
  #   # Run every hour
  #   - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  call-performance-tests:
    if: github.actor == github.repository_owner
    uses: ./.github/workflows/tracker-performance-tests.yml
    with:
      runner-type: buildjet-2vcpu-ubuntu-2204
      share-connections: true
